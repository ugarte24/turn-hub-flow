#!/bin/sh
# Auto-increment PATCH version on every commit.
cd "$(git rev-parse --show-toplevel)" || exit 1
node scripts/bump-version.mjs patch || exit 1
git add package.json src/lib/version.ts docs/PRD.md
